[package]
name = "snout-cli"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4.6.1", features = ["derive"] }
snout = { path = "../.." }
tracing-subscriber = { version = "0.3", features = ["env-filter"] }
